# Enable raw MQTT for internal use (optional)
listener 1883
protocol mqtt

# Expose MQTT-over-WebSockets for public access
listener 9001
protocol websockets

allow_anonymous true

# Persistence (optional)
persistence true
persistence_location /mosquitto/data/
