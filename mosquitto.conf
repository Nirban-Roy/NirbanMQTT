# Raw MQTT only on localhost (optional – TCP 1883)
listener 1883 127.0.0.1
protocol mqtt

# WebSockets for Mosquitto (internal only)
listener 1884
protocol websockets

allow_anonymous true

# Persistence
persistence true
persistence_location /mosquitto/data/
