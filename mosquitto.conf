# Raw MQTT – only inside container (bound to localhost)
listener 1883 127.0.0.1
protocol mqtt

# MQTT-over-WebSockets – public via Render’s HTTPS/WSS proxy
listener 9001
protocol websockets

allow_anonymous true

# Persistence (optional but recommended)
persistence true
persistence_location /mosquitto/data/
