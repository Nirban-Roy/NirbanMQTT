services:
  - type: web             # HTTP & WSS routing
    name: NirbanMQTT
    runtime: docker       # build via your Dockerfile
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT         # route external 443 → container 9001
        value: "9001"
